# CRITICAL: Data Integrity Issue - FFT Spectrum

## 🚨 PROBLEM IDENTIFIED

The FFT Spectrum Display is showing **FAKE DATA** generated by placeholder algorithms:

### Fake Data Sources Found:
1. **`/api/fft` endpoint** (server-enhanced.js:2127-2191)
   - Generates fake spectrum using sine waves
   - Comment: "TODO: Use actual FFT from DSP pipeline"
   - Returns fabricated power values

2. **`DSP.computeFrequencyBands()`** (dsp.js:393-420)
   - Returns RMS-based estimates, not real FFT
   - Simple heuristic: "lower frequencies = higher power"
   - Not scientifically valid

3. **FFT Spectrum View** (index.html:3778+)
   - Displays the fake data from /api/fft
   - Visually appealing but scientifically INVALID

## ⚠️ IMPACT

**This violates scientific research standards:**
- Data shown is NOT from real signal analysis
- Could lead to incorrect conclusions
- Undermines credibility of the entire system
- Unacceptable for research/clinical use

## ✅ IMMEDIATE ACTION REQUIRED

### Option 1: Disable FFT Spectrum View (Quick Fix)
- Remove FFT Spectrum from view menu
- Add "Coming Soon - Real FFT Implementation" message
- Use only Band Powers view (which uses REAL filtering)

### Option 2: Implement Real FFT (Proper Fix)
Requires:
1. Add FFT library (fft.js, kiss-fft, or implement Cooley-Tukey)
2. Collect EEG samples into windows (512, 1024, 2048 samples)
3. Apply window function (Hanning, Hamming)
4. Compute real FFT
5. Convert to power spectral density
6. Return actual frequency bins

### Option 3: Use NeurOSC's Approach
- Study NeurOSC's Python FFT implementation
- Port to JavaScript or call Python backend
- Use scipy.signal.welch() for proper PSD estimation

## 🔬 WHAT DATA IS REAL vs FAKE

### ✅ REAL DATA (Safe to Use):
- **Raw EEG traces** - Direct from hardware/simulator
- **Band Powers from filtering** - Uses real Butterworth filters
- **All Bands Timeline** - Real band-filtered power
- **Traces View** - Raw voltage samples
- **Band Powers View** - Real filter bank output

### ❌ FAKE DATA (Must Remove):
- **FFT Spectrum** - Placeholder sine waves
- **DSP.fft array** - Not real FFT

## 📋 IMPLEMENTATION PLAN

### Phase 1: Remove Fake Data (IMMEDIATE)
- [ ] Comment out FFT Spectrum view
- [ ] Add warning message
- [ ] Document in UI: "FFT requires real implementation"

### Phase 2: Real FFT Implementation
- [ ] Choose FFT library (fft.js recommended)
- [ ] Implement windowing (Hanning)
- [ ] Compute PSD (Power Spectral Density)
- [ ] Add proper frequency binning
- [ ] Validate against known signals

### Phase 3: Data File Playback
- [ ] Import EDF/BDF files (EEG standard format)
- [ ] Import CSV files (OpenBCI format)
- [ ] Import NeurOSC recordings
- [ ] Playback controls (play/pause/seek)
- [ ] OSC output during playback
- [ ] Loop/speed controls

## 🎯 USER REQUIREMENTS

From user feedback:
1. ✅ Simulator can use simulated data (acceptable)
2. ❌ NO fake data in scientific/research interface
3. 🔄 Need to read brainwave data files from web
4. 🔄 Need to visualize imported files
5. 🔄 Need to sonify via OSC (playback → OSC output)

## 📚 REFERENCES

**Real FFT Libraries:**
- fft.js: https://github.com/indutny/fft.js
- kiss-fft: Small, fast, portable
- Web Audio API: Built-in FFT (AnalyserNode)

**EEG File Formats:**
- EDF (European Data Format): Most common
- BDF (BioSemi): 24-bit variant
- CSV: OpenBCI/custom formats
- JSON: NeuroVis/NeurOSC recordings

**NeurOSC Reference:**
- Python: scipy.signal.welch() for PSD
- Uses proper windowing and averaging
- Scientifically validated approach

## 🚨 STATUS

**Current State:** UNSAFE FOR RESEARCH  
**Action Taken:** Documenting issue  
**Next Step:** Disable fake FFT view  
**Timeline:** Implement real FFT in next sprint  

---

**This document tracks the critical data integrity issue and resolution plan.**
